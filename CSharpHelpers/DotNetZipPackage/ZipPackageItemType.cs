namespace CSharpHelpers.DotNetZipPackage
{
    /// <summary>
    /// Identifies the package item type. Used to describe the content of the file so that it can be parsed appropriately
    /// </summary>
    public enum ZipPackageItemType
    {
        HTMLFile = 1,
        Graphic = 2,
        Script = 3,
    }
}
